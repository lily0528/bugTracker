@model EditProject
@{
    ViewBag.Title = "EditProject";
}

<h2>EditProject</h2>

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
<form method="post">
    <div class="form-group">
        @Html.LabelFor(p => p.Name, "Project Name:")
        @Html.TextBoxFor(p => p.Name, new { @class = "form-control", placeholder = "Project Name" })

    </div>
    <div class="form-group">
        @Html.LabelFor(p => p.Description, "Description")
        @Html.TextAreaFor(p => p.Description, new { @class = "form-control", placeholder = "Description", rows = 5, columns = 5 })
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
